# Secure file permissions baseline. Format: /path/to/file MODE
# The harden script will apply these. The audit script will check against them.
# The format is: [file_path] [permission_mode]

/etc/ssh/sshd_config 600
/etc/shadow 640
/etc/gshadow 640
/etc/sudoers 440
/etc/passwd 644
/etc/group 644
